import { createTheme } from '@mantine/core';

/** Theme de BioWellness, compartido con el resto de las apps (`recepcionistas`, etc.). */
export const theme = createTheme({
  primaryColor: 'teal',
  defaultRadius: 'md',
});
